///////////////////////////////////////////////////////////////////////////////////////////////////
//// SECTION -> Pre-Processor (Non-Defs)
///////////////////////////////////////////////////////////////////////////////////////////////////

#pragma once

///////////////////////////////////////////////////////////////////////////////////////////////////
//// SECTION -> Constant Settings
///////////////////////////////////////////////////////////////////////////////////////////////////

#define GEN_DEBUG_MODE 1 // 1 = Debug mode -> print things, 0 = Standard mode -> dont print. 

#define SETTINGS_AMOUNT_MAX 16 // Maximum number of settings.

#define ERRORS_AMOUNT_MAX 6 // Maximum number of errors that can be stored at one time.
#define ERRORS_ASSERT_RESET 1 // 1 = Will restart on valid assert/deny, 0 = wont restart.
#define ERRORS_ASSERT_RESTART_DELAY 500 // Restart delay in valid asser/deny (milliseconds)

///////////////////////////////////////////////////////////////////////////////////////////////////
//// SECTION -> Communication Protocol
///////////////////////////////////////////////////////////////////////////////////////////////////

#define PRTC_NULL 0


///////////////////////////////////////////////////////////////////////////////////////////////////
//// SECTION -> Enums
///////////////////////////////////////////////////////////////////////////////////////////////////

enum SETTING_ID : uint8_t {
  SETTING_NULL = PRTC_NULL
};
 
enum ERROR_TYPE : uint8_t {
  ERR_NULL = PRTC_NULL
};

enum BOARD_RESTART_MSG : uint8_t {
  RST_NULL = PRTC_NULL
};

